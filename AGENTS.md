# 机器人项目开发说明与 Codex 工作规则

## 1. 文档作用

本文件是本仓库对 Codex 和其他自动化开发代理的长期项目说明。

进入仓库后，应先阅读本文件，再阅读 `README.md`、`docs/`、当前任务说明和相关源码。

本文件用于明确：

- 项目的开发目的和非商业边界；
- 初步技术架构与开发顺序；
- 安全、隐私和内容边界；
- 允许自动执行的操作；
- 必须由用户确认的操作；
- 修改、测试、审查和交付规范。

如果用户在当前对话中给出更具体的指令，以用户当前明确指令为准，但不得绕过系统安全边界。

---

## 2. 项目性质与开发目的

这是一个部署在个人轻量 Ubuntu 服务器上的聊天机器人学习项目。

项目仅用于：

- 个人学习；
- 技术研究；
- 自己管理的 QQ 小号、私有群和 KOOK 测试服务器；
- 少量熟人之间的非商业交流；
- 验证聊天平台接入、游戏查询、大模型对话、长期记忆和媒体处理等技术。

项目不用于：

- 商业运营；
- 付费服务；
- 面向公众的大规模上线；
- 营销、引流、广告或批量私信；
- 批量注册、批量养号或绕过平台风控；
- 未经授权抓取、传播或存储受保护内容；
- 绕过付费、DRM、访问控制或平台权限；
- 违法、欺诈、骚扰、隐私侵犯或其他滥用行为。

如果项目范围从“个人学习、非商业、私有测试”发生变化，必须暂停扩展并重新评估：

- 平台规则；
- 开源许可证；
- 隐私与数据处理；
- 内容安全；
- 账号和接口合规性；
- 部署与运营责任。

---

## 3. 目标功能

### 3.1 聊天平台

计划支持：

- QQ 群聊和私聊；
- KOOK 群聊和私聊；
- 后续通过统一事件模型扩展其他平台。

QQ 初期使用：

- 专用 QQ 小号；
- NapCatQQ；
- OneBot 11；
- 反向 WebSocket；
- TRSS-Yunzai 作为应用层和插件宿主。

QQ 不使用官方机器人接口，但必须接受以下现实边界：

- 社区协议端可能随 NTQQ 更新失效；
- 可能触发登录验证、风控或封号；
- 协议端必须可替换；
- 不得使用主 QQ；
- 不得保存 QQ 明文密码；
- 不得将 NapCat WebUI、OneBot HTTP 或 WebSocket 端口直接暴露公网。

KOOK 初期原则：

- KOOK 使用正常的 Bot Token 和事件接入；
- 优先复用 TRSS-Yunzai 的 KOOK 适配器；
- 如果用户坚持 KOOK 也不使用官方 Bot 接入，则暂缓 KOOK，不实现用户号 selfbot。

### 3.2 游戏账号查询

计划支持：

- 原神角色、深渊、练度和公开信息查询；
- 崩坏：星穹铁道角色、练度和公开信息查询；
- 后续扩展其他游戏。

初期候选组件：

- TRSS-Yunzai；
- miao-plugin；
- GSUIDCore；
- GenshinUID；
- StarRailUID。

优先实现公开 UID 查询。

需要 Cookie、扫码授权或其他敏感凭据的功能必须后置，并满足：

- 只允许私聊绑定；
- 不在群内回显凭据；
- 不记录明文 Cookie；
- 日志必须脱敏；
- 支持解绑和删除；
- 插件故障不得拖垮主机器人；
- 外部接口失败时应明确降级。

### 3.3 大模型聊天

计划实现：

- 一对一私聊；
- 群聊中被 `@`、引用或命令触发时进行轻量对话；
- 每个平台用户拥有独立会话；
- 支持模型供应商替换；
- 支持对话超时、重试、限流和降级；
- 后续发展为健康的情感陪伴型机器人。

首版不做：

- 无限制主动私信；
- 群聊所有消息监听并自动插话；
- 跨平台用户身份自动合并；
- 不可解释的永久记忆；
- 依靠操控、嫉妒、自伤威胁或排他性话术维持用户使用；
- 将好感度作为获得基本尊重和帮助的条件。

### 3.4 长期记忆与关系系统

后续计划实现：

- 每个用户独立的短期上下文；
- 用户事实记忆；
- 共同事件记忆；
- 可选的角色日记；
- 好感度、信任度和短期情绪；
- 人格阶段；
- 用户查看、纠正、删除和重置记忆；
- 记忆来源、置信度、重要度和保留时间；
- 私聊记忆与群聊记忆隔离。

关系系统原则：

- 好感度只影响称呼、主动性、剧情和表达风格；
- 好感度不得影响基本尊重、安全回应和用户权益；
- 不通过羞辱、威胁、情感勒索或自伤暗示推动关系；
- 不鼓励用户疏远现实中的家人、朋友或专业支持；
- 不建立未成年人性化内容；
- 儿童或幼儿人格必须与恋爱、亲密和性内容完全隔离；
- 用户真实危机表达应退出角色扮演，进入安全响应流程。

### 3.5 视频链接解析

计划实现：

- 识别白名单站点的公开分享链接；
- 提取标题、作者、封面和基础元数据；
- 下载公开可访问的视频；
- 必要时使用 FFmpeg 转封装；
- 按 QQ/KOOK 能力上传；
- 超限或失败时降级为“封面 + 原链接 + 错误说明”。

首版限制：

- 只支持 1 到 2 个经过测试的站点；
- 默认最大文件 50 MB；
- 默认最长视频 10 分钟；
- 禁止直播和超长播放列表；
- 禁止付费内容和 DRM；
- 禁止访问内网、云元数据地址和本机管理接口；
- 临时文件必须设置 TTL 并清理；
- 不得把用户提供的 URL 直接拼进 Shell 命令。

---

## 4. 初步技术架构

推荐的初步架构：

```text
QQ 专用小号
    │
    ▼
NapCatQQ
    │ OneBot 11 反向 WebSocket
    ▼
TRSS-Yunzai
    ├── 平台事件与消息发送
    ├── 权限、群白名单和命令路由
    ├── miao-plugin
    ├── 游戏查询适配插件
    ├── AI 侧车调用插件
    └── 媒体任务提交插件

KOOK Bot
    │
    └── TRSS-Yunzai KOOK 适配器

独立 Python 服务
    ├── AI 对话 API
    ├── 用户会话隔离
    ├── 人格和关系状态
    ├── 长期记忆
    ├── GameQuery 统一接口
    └── 媒体解析任务

持久化
    ├── 原型期：SQLite / 文件配置
    ├── 记忆期：PostgreSQL + pgvector
    ├── 任务增多后：Redis 或兼容队列
    └── 媒体：本地临时目录，短 TTL
```

### 4.1 架构边界

- TRSS-Yunzai 负责平台、插件和游戏生态。
- Python 服务负责 AI、记忆、关系状态和媒体隔离。
- 不把长期记忆、游戏 Cookie、模型密钥和下载器全部塞进同一 Yunzai 插件。
- 游戏查询通过统一 `GameQuery` 接口调用，不让 AI 核心依赖具体插件内部实现。
- 媒体 worker 不得拥有游戏 Cookie、模型密钥或数据库管理员权限。
- 游戏查询组件不得拥有 QQ/KOOK 发送凭据以外的无关权限。
- 数据库是状态真相源，Redis 不得成为唯一持久化来源。

### 4.2 轻量服务器策略

服务器资源有限，初期应避免一次性部署完整重型栈。

优先顺序：

1. 先跑通 NapCatQQ、OneBot 11 和 TRSS-Yunzai。
2. AI 侧车先使用进程内队列和轻量数据库。
3. 没有长期记忆需求前，不强制安装 pgvector。
4. 没有明显并发和异步积压前，不强制安装 Redis。
5. 没有多实例需求前，不部署 Kubernetes。
6. 可先使用 systemd 管理进程，确有需要再引入 Docker Compose。
7. 媒体转码必须限制 CPU、并发、文件大小和执行时间。

---

## 5. 初步开发计划

开发遵循“先跑通最小纵向闭环，再逐步增加高风险功能”的原则。

### 阶段 0：仓库与服务器基线

目标：

- 建立 Git 仓库；
- 完成 `README.md`、`AGENTS.md` 和基础 `docs/`；
- 记录 Ubuntu、Node.js、Python、Codex、TRSS 和 NapCat 版本；
- 建立环境变量模板；
- 建立日志目录、备份目录和测试目录；
- 明确开发启动和停止命令；
- 禁止密钥进入 Git。

交付物：

- `README.md`
- `AGENTS.md`
- `.gitignore`
- `.env.example`
- `docs/architecture.md`
- `docs/deployment.md`
- `docs/security.md`
- `scripts/health-check.sh`
- `scripts/backup.sh`

验收：

- `git status` 干净；
- 仓库不含 Token、Cookie、密码和私钥；
- 能明确说明如何启动、停止、测试和查看日志。

### 阶段 1：QQ 最小闭环

目标链路：

```text
NTQQ → NapCatQQ → OneBot 11 反向 WebSocket
→ TRSS-Yunzai → 收到消息 → 返回文本或图片
```

要求：

- 使用专用 QQ 小号；
- NapCat WebUI 仅监听本机或私有网络；
- OneBot 启用 access token；
- TRSS 只响应白名单用户和白名单群；
- 群聊默认只响应 `@`、引用和命令；
- 测试断线重连、重复事件和发送失败。

验收：

- 私聊收发正常；
- 白名单群收发正常；
- 白名单外群不响应；
- 图片发送正常；
- 重连后不重复发送；
- 公网扫描不到 NapCat WebUI 和 OneBot 端口。

### 阶段 2：TRSS 与游戏查询

目标：

- 安装并固定 TRSS-Yunzai 及相关插件版本；
- 先接 miao-plugin 验证原神面板；
- 再评估 GSUIDCore、GenshinUID 和 StarRailUID；
- 建立统一 GameQuery 适配层。

顺序：

1. 公共 UID 查询；
2. 原神基础查询；
3. 星铁基础查询；
4. 错误处理和超时；
5. 私聊绑定；
6. Cookie 加密、解绑和删除。

验收：

- 原神至少 3 个稳定命令；
- 星铁至少 3 个稳定命令；
- 查询失败不会阻塞机器人；
- Cookie 不出现在群消息、普通日志或异常堆栈中；
- 插件升级前可备份和回滚。

### 阶段 3：AI 私聊侧车

目标：

- 建立独立 Python AI 服务；
- TRSS 自定义插件通过受限 HTTP/WebSocket 调用；
- 实现供应商抽象；
- 每个用户独立短期上下文；
- 群聊默认只有明确触发才调用模型。

首版只实现：

- 私聊多轮文本；
- 用户会话隔离；
- 超时和重试；
- 每用户限流；
- 模型不可用时的固定降级回复；
- 上下文重置命令；
- 基础内容安全。

暂不实现：

- 主动召回；
- 永久记忆；
- 跨平台合并；
- 复杂好感度；
- 模型自主执行系统命令。

验收：

- 至少完成 50 组多轮测试；
- 不同用户上下文不串线；
- 模型断线不会拖垮 TRSS；
- 用户可以重置自己的上下文；
- 群内其他人的消息不会进入当前用户私聊记忆。

### 阶段 4：长期记忆与健康人格

目标：

- 将短期上下文、事实记忆、事件记忆和角色日记分层；
- 引入 PostgreSQL + pgvector，或先完成可迁移的数据接口；
- 为每个用户建立独立 namespace；
- 实现好感、信任、情绪和人格阶段；
- 实现记忆查看、纠正、删除和重置。

数据要求：

- 每条记忆有唯一 ID；
- 有来源、时间、类型、置信度和重要度；
- 用户明确事实优先于模型推断；
- 角色日记标记为主观记忆；
- 敏感记忆默认不长期保存；
- 群聊默认不建立长期个人记忆；
- 备份中的删除策略必须明确。

验收：

- 跨用户记忆泄漏为零；
- 错误记忆可以纠正；
- 用户可以删除所有个人记忆；
- 人格切换不会混入不兼容关系历史；
- 好感度不会触发操控、自伤威胁或未成年人性化内容。

### 阶段 5：视频解析 worker

目标：

- 建立独立媒体 worker；
- 实现 URL 规范化和域名白名单；
- 提取封面与元数据；
- 使用 yt-dlp 或站点专用解析器；
- 使用 FFmpeg 转封装；
- 上传前检查平台大小限制；
- 完成失败降级和临时文件清理。

安全测试：

- `127.0.0.1`
- `localhost`
- IPv6 loopback
- RFC1918 私网地址
- 云元数据地址
- DNS 重绑定
- 超大文件
- 超长视频
- 恶意文件名
- 播放列表爆炸
- 伪造 MIME
- 重定向到内网

验收：

- 只支持明确白名单站点；
- SSRF 和命令注入测试通过；
- 资源超限能快速停止；
- 失败时返回封面和原链接；
- 临时文件按 TTL 全部清理；
- 不绕过付费、DRM 和访问限制。

### 阶段 6：KOOK 与多平台统一

前置条件：

- QQ 链路稳定；
- 消息事件模型已经抽象；
- AI、游戏和媒体不直接依赖 QQ 消息格式。

目标：

- 接入 KOOK Bot；
- 复用同一 AI、游戏和媒体服务；
- 处理 KOOK 资源上传、速率限制和消息格式差异；
- 不通过昵称自动合并 QQ 和 KOOK 身份。

验收：

- KOOK 文本和媒体收发正常；
- 正确处理 429 和重试等待；
- QQ 与 KOOK 用户默认是不同身份；
- 跨平台合并需要用户双端确认。

### 阶段 7：私有测试稳定化

目标：

- systemd 或 Docker Compose 自启动；
- 日志轮转；
- 健康检查；
- 备份和恢复；
- 硬预算和限流；
- 功能开关；
- 一键停用 QQ、AI、游戏和媒体组件；
- 连续运行测试。

验收：

- 连续 7 天低频运行；
- 服务崩溃可恢复；
- 备份可实际恢复；
- QQ 协议升级可先在测试小号验证；
- 能单独停用 AI、游戏或媒体功能；
- 重要日志中不出现敏感凭据。

---

## 6. MVP 范围

首个可用版本只包含：

- QQ 单平台；
- NapCatQQ + OneBot 11 + TRSS-Yunzai；
- 私聊和白名单群；
- 帮助、状态和基础管理命令；
- 原神和星铁少量公开查询；
- AI 私聊短期上下文；
- 群聊只在 `@`、引用或命令时调用 AI；
- 1 到 2 个视频站点；
- 最大 50 MB、最长 10 分钟；
- 基础日志、备份和停止脚本。

MVP 不包含：

- KOOK；
- 主动私信；
- 永久记忆；
- 跨平台身份合并；
- 自动生产部署；
- 大规模群运营；
- 全站视频解析；
- 绕过平台限制；
- 大量游戏 Cookie 托管。

---

## 7. Codex 工作权限

### 7.1 默认允许

Codex 可以在当前仓库内：

- 读取文件；
- 搜索源码；
- 分析架构；
- 创建和修改项目文件；
- 编写文档；
- 编写测试；
- 运行格式化、静态检查和测试；
- 查看 `git status`；
- 查看 `git diff`；
- 创建普通 Git 分支；
- 在临时目录生成构建产物；
- 查看经过授权且已经脱敏的项目日志；
- 提出部署方案但不直接执行生产部署。

### 7.2 必须先询问用户

执行下列操作前必须获得用户明确授权：

- 使用 `sudo`；
- 安装或卸载系统软件包；
- 修改 systemd 服务；
- 启动、停止或重启生产机器人；
- 修改 SSH、防火墙、端口和系统用户；
- 修改 `/etc`、`/var/lib`、`/var/log` 等项目外路径；
- 修改数据库 schema 或运行生产迁移；
- 读取生产数据库；
- 读取或修改真实 Cookie、Token、密码和 API Key；
- 登录 QQ、KOOK、米游社或其他第三方账号；
- 向真实群、真实用户或外部服务发送消息；
- `git push`；
- 创建 Pull Request；
- 合并分支；
- 发布版本；
- 上传文件；
- 删除数据；
- 清理 Docker 镜像、卷和容器；
- 扩大 Codex 沙箱或网络权限；
- 执行不可逆或影响生产状态的操作。

### 7.3 默认禁止

除非用户对精确目标作出明确、单次授权，否则不得：

- 使用 `git reset --hard`；
- 使用 `git checkout --` 覆盖用户改动；
- 对宽泛目录执行递归删除；
- 删除仓库、家目录、数据库或备份；
- 删除 Docker volume；
- 覆盖 `.env`、密钥文件或生产配置；
- 关闭防火墙或全局安全限制；
- 禁用 AppArmor；
- 开放 NapCat WebUI、OneBot 或数据库到公网；
- 将任意群聊文本直接作为 Shell 命令；
- 把用户 URL 直接拼接进 Shell；
- 绕过 DRM、付费墙或访问控制；
- 代表用户接受协议、付费、订阅或购买服务；
- 把凭据写进代码、日志、提交、镜像或回复。

---

## 8. 凭据与隐私规则

以下内容属于高敏感信息：

- QQ Cookie、扫码会话和登录 Token；
- KOOK Bot Token；
- OneBot access token；
- 模型 API Key；
- 米游社 Cookie 和设备信息；
- 数据库密码；
- SSH 私钥；
- `~/.codex/auth.json`；
- 用户私聊记录；
- 长期记忆；
- 用户绑定的游戏账号信息。

规则：

- 不读取与当前任务无关的敏感文件；
- 不在终端输出完整凭据；
- 日志中只显示脱敏形式；
- 不将敏感文件加入 Git；
- `.env.example` 只能放字段名和假值；
- 异常堆栈不得包含请求头、Cookie 或完整请求体；
- 测试使用虚构数据；
- 不把生产数据复制到测试环境；
- 不允许跨用户查询和跨用户记忆召回；
- 用户删除后应同时处理缓存、数据库和明确范围内的备份策略。

如果发现疑似泄露：

1. 立即停止继续输出；
2. 不复制或扩散敏感值；
3. 告知用户泄露路径；
4. 建议轮换相关凭据；
5. 检查 Git 历史、日志、镜像和备份；
6. 未经授权不得自行修改外部账号。

---

## 9. 网络与媒体安全

- 所有外部 URL 默认不可信。
- 下载器必须使用域名白名单。
- DNS 解析前后都要检查目标地址。
- 禁止访问 loopback、私网、链路本地、云元数据和管理网段。
- 限制重定向次数。
- 限制下载大小、持续时间、并发和超时。
- 禁止使用字符串拼接执行命令。
- 调用 FFmpeg、yt-dlp 等工具时使用参数数组。
- 临时目录按任务隔离。
- 文件名必须重新生成，不信任远端文件名。
- 上传前检测 MIME、扩展名和实际内容。
- 媒体 worker 不得访问数据库管理员账户和其他服务密钥。

---

## 10. Git 与修改流程

开始任务前：

1. 阅读本文件和相关文档。
2. 执行 `git status --short`。
3. 识别用户已有修改。
4. 不覆盖、不删除、不格式化无关改动。
5. 说明任务范围和假设。

修改过程中：

- 只做与任务直接相关的改动；
- 优先小步提交和可回滚设计；
- 不无故更换框架；
- 不无故升级全部依赖；
- 不把调试密钥和真实用户数据加入测试；
- 不把两个完整机器人框架同时接管同一消息源；
- 对协议、游戏、AI 和媒体使用清晰适配层。

修改完成后：

1. 运行最相关的格式化和测试。
2. 执行 `git diff --check`。
3. 查看 `git status --short`。
4. 总结修改文件。
5. 报告测试结果。
6. 报告仍存在的风险和未验证项。
7. 不自动部署，不自动 push。

---

## 11. 测试要求

至少覆盖：

- 单元测试；
- 平台事件契约测试；
- 重复消息和幂等；
- 群聊与私聊隔离；
- 不同用户上下文隔离；
- 游戏查询超时和错误；
- 模型超时和降级；
- 记忆写入、召回、纠正和删除；
- 视频 SSRF；
- 恶意文件名；
- 超大文件和超长视频；
- 临时文件清理；
- 生产凭据脱敏；
- 服务重启和断线重连。

重要验收底线：

- 跨用户数据泄漏为零；
- 凭据泄漏为零；
- NapCat/OneBot 公网暴露为零；
- URL 访问内网为零；
- 未经确认的生产变更为零。

---

## 12. 常用命令

以下命令在对应脚本实际存在后才能使用。若脚本不存在，先报告，不得猜测命令。

```bash
# 查看状态
git status --short

# 查看改动
git diff

# 运行项目测试
./scripts/test.sh

# 健康检查
./scripts/health-check.sh

# 查看开发日志
./scripts/dev-logs.sh

# 备份
./scripts/backup.sh

# 启动开发环境
./scripts/dev-start.sh

# 停止开发环境
./scripts/dev-stop.sh
```

生产部署、生产重启和恢复命令不得由 Codex 在没有用户明确授权时执行。

---

## 13. 决策优先级

遇到冲突时，按以下顺序处理：

1. 用户安全与数据安全；
2. 用户当前明确指令；
3. 本文件中的禁止项与审批要求；
4. 保持现有项目可运行；
5. MVP 范围；
6. 简单、可测试、可替换的实现；
7. 性能和功能扩展；
8. 代码风格偏好。

如果信息不足：

- 优先进行只读检查；
- 能安全验证的先验证；
- 不对生产环境作高影响猜测；
- 需要扩大权限、接触凭据或改变架构时停止并询问用户。

---

## 14. 当前默认结论

- 主应用层：TRSS-Yunzai。
- QQ 协议端：NapCatQQ + OneBot 11 反向 WebSocket。
- QQ 备选协议端：LLBot。
- KOOK：在 QQ 稳定后接入正常 Bot。
- 游戏：miao-plugin 起步，GSUIDCore/GenshinUID/StarRailUID 后续评估。
- AI：独立 Python 服务，不直接塞进主插件进程。
- 记忆：先短期上下文，后长期记忆。
- 数据库：轻量原型可先简化，长期记忆阶段再引入 PostgreSQL + pgvector。
- Redis：有明确队列、限流或并发需求后再引入。
- 媒体：独立 worker，严格白名单和资源限制。
- 部署：个人轻量 Ubuntu 服务器，初期优先 systemd 或简单进程管理。
- 账号：专用 QQ 小号。
- 群范围：私有白名单群。
- 使用性质：个人学习、非商业、非公开运营。
