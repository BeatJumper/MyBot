# Copy only the fields needed by a component into its local environment file.
# Never commit real credentials.

ONEBOT_ACCESS_TOKEN=replace-with-a-random-token
AI_PROVIDER_API_KEY=replace-with-your-api-key
AI_SERVICE_BASE_URL=http://127.0.0.1:8000
